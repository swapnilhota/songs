import React from 'react';
import { selectSong } from '../actions';  // not necessary to write file name because the filename is index.js

const App = () => {
    return (
        <div>App</div>
    );
}

export default App;